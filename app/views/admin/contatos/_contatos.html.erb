<table class="grid">
  
  <tr class="header">
    <th colspan="4"> <%= title %> </th>
  </tr>

  <% if contatos.present? %>
    <%= render contatos %>
  <% else %>
    <tr><td class="hint">Não há mensagens...</td></tr>
  <% end %>
  
</table>

<br/>